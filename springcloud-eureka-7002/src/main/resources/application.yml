server:
  port: 7002

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false  #表示是否注册
    fetch-registry: false #表示自己是否是注册中心
    service-url:
      defaultZone: http://localhost:7001/eureka/,http://localhost:7003/eureka/
